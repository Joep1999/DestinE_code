from setuptools import setup, find_packages

setup(
    name="pysteps_destine",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "numpy",
        "scipy",
        "matplotlib",
        # add other dependencies as needed
    ],
)
